# Kalie Social Network API

API backend completa para a rede social Kalie, construída com Node.js, TypeScript, Prisma e MongoDB.

## 🚀 Começando

### Pré-requisitos
- Node.js 18+
- MongoDB (local ou Atlas)
- npm ou yarn

### Instalação

1. Clone o repositório
```bash
git clone <repository-url>
cd kalie-backend